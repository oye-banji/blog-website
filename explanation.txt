local host
- like a domain name on a web(like google) but this one takes us back to a loop back function which loops us back to our own computer using the IP address 127.0.0.1

PORT NUMBERS
- like doors into a cumputer that softwares use to comunicate with our computer using unique port numbers

MIDDLEWARE
- Code which runs (on the server) between getting a request and sending a response

REQUEST TYPES

GET: requests to get a resource
POST: requests to create new data (e.g. a new blog)
DELETE: requests to delete data (e.g. delete a blog)
PUT: requests to update data (e.g. update a blog)

ROUTE PARAMETERS
    - The vaariable parts of the route that may change value
